server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true


spring:
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true
    thymeleaf:
      cache: false

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tarottarot?serverTimezone=Asia/Seoul
    username: root
    password: kwon7788966

  jpa:
    properties:
      hibernate:
        ddl-auto: none
        show-sql: true
        format_sql: true
